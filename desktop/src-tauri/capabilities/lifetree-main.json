{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "lifetree-main",
  "description": "Permissions for the main LifeTree window. Grants notification capability so risk alerts can be delivered to the OS notification center. Works in both local private mode and remote instance mode (self-hosted / cloud).",
  "local": true,
  "remote": {
    "urls": ["https://*", "http://localhost:*", "http://127.0.0.1:*"]
  },
  "windows": ["lifetree"],
  "permissions": [
    "core:default",
    "notification:default",
    "notification:allow-notify",
    "notification:allow-request-permission",
    "notification:allow-is-permission-granted",
    "notification:allow-permission-state",
    "opener:default",
    "opener:allow-open-url"
  ]
}

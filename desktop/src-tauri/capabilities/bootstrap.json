{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bootstrap",
  "description": "Minimum permissions for the local desktop bootstrap window.",
  "local": true,
  "windows": ["bootstrap"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-url"
  ]
}

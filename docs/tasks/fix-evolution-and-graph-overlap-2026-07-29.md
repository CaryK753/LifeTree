# 修复自演化并区分证据关系图与决策树

日期：2026-07-29
作者：wwj

## TODO

- [x] 暂停发布，确认失败请求和接口范围。
- [x] 提高决策树演化对结构化输出波动的容错能力。
- [x] 阻止未确认分支继续演化并抑制重复子分支。
- [x] 改善单节点及“探索全部”的错误反馈。
- [x] 将关系图收敛为跨实体证据关系图，补齐信源证据边。
- [x] 修复发布前静态检查发现的未定义类型和路径状态契约漂移。
- [x] 运行测试、类型检查和浏览器验收。
- [ ] 验收通过后提交、推送、等待镜像构建并部署 Spark。

## 职责边界

- 证据关系图：回答“哪些目标、要求、风险、事件和信源彼此关联，证据从哪里来”。
- 决策树：回答“一个目标有哪些可选路径，概率如何，哪些分支应确认、放弃或继续演化”。

## 验收记录

- 对此前返回 502 的两个根路径重新执行“探索全部”，均返回 200，分别生成 4 条和 3 条预测分支。
- 未确认的 `predicted` 分支不再进入下一轮“探索全部”；后端同时拒绝直接递归演化。
- `GET /goals/{id}` 可序列化完整的路径状态，树标题恢复为目标名称。
- 决策树重新加载后无 React Flow 根边 handle 警告。
- 证据关系图仅保留目标筛选，当前实测 107 个节点、210 条关系，无控制台错误。
- 证据关系图在 390×844 下文档与主区域宽度均为 390px，无横向溢出。

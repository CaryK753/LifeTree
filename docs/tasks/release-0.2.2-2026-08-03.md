# 0.2.2 发布收尾

- 日期：2026-08-03
- 作者：wwj
- 状态：待 CI 验收

## 目标

- macOS 菜单栏使用白色 LifeTree 图标；菜单栏和 Windows 托盘的手动更新检查使用原生系统提示。
- 将 Advisor 每轮工具调用上限提升至 128，并确认行动日历工具可读写日程。
- 生成 `0.2.2` 变更记录、签名桌面更新产物并发布版本标签。

## TODO

- [x] 接入原生更新检查提示并生成 macOS 白色图标。
- [x] 将工具调用上限调整为 128，确认日历工具已注册并有测试覆盖。
- [x] 更新版本号与 `CHANGELOG.md`。
- [x] 重建并验证桌面更新包。
- [ ] 提交、推送并创建 `v0.2.2` 标签（推送后由 GitHub Actions 验收）。

## 验证

- 后端全量测试：`103 passed`。
- 前端生产构建与桌面静态导出通过。
- 桌面 Rust 测试、Clippy、完整 `.app` / `.dmg` / 签名 updater 产物通过。
- `docker compose config --quiet` 与 `git diff --check` 通过。

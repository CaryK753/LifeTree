# 图谱、目标、行动日历与推理修复

日期：2026-07-29

## 范围

- 调整 `/graph` Canvas 尺寸，避免超出视口。
- 修复场景自演化接口 500。
- 新建目标默认状态改为 `active`（进行中）。
- 为 AI 提供查看和更新行动日历的内置工具。
- 阻止模型重复创建同一目标，优先复用现有目标。
- 修复 `/dashboard` 变更摘要加载失败。
- 修复所有分支固定显示 P50 32%、风险 68% 的推理问题。

## TODO

- [x] 定位六项问题的调用路径、接口契约与现有改动。
- [x] 实现前后端最小修复。
- [x] 补充重复目标、日历工具和推理算法回归测试。
- [x] 运行后端测试、前端检查并启动服务验证。

## 验证结果

- 数据库已迁移至 `i2b3c4d5e6f7 (head)`，6 张新增表可用。
- `GET /api/v1/changes-summary` 已认证请求返回 200。
- 场景/决策树两条自演化上下文加载成功；未触发付费 LLM 生成。
- 等价目标 POST 前后目标数保持 `4 -> 4`。
- 行动日历工具查询成功。
- 算法断言：低风险分支 P50 0.6145、风险 0.018；高风险分支 P50 0.2392、风险 0.54。
- `npm run type-check` 与 `npm run build` 通过。
- `/graph` 桌面 `1864x848`、移动 `390x844` 均无页面溢出。

## 验收标准

- `/graph` 在桌面与移动视口内无横向或纵向溢出。
- 自演化请求不再因服务内部错误返回 500，并保留可诊断错误信息。
- API、AI 工具新建的目标状态均为 `active`。
- AI 可按日期范围读取行动，并更新行动日期、状态等日历字段。
- 相同用户的等价目标创建请求返回已有目标，不新增重复记录。
- `/dashboard` 能正确显示空态或变更摘要。
- 不同分支输入产生可区分的 P50 和风险评分，默认值不再机械互补。

# LifeTree 全局产品优化

- 日期：2026-07-28
- 状态：进行中
- 作者：wwj

## TODO

- [x] 优化 `/auth` 昼夜 ASCII 场景：月亮、流星、太阳、云、鸟、路灯、长椅、人物
- [x] 新增 Terms / Privacy 公共页面及全站链接
- [x] 注册与 OAuth 注册增加协议同意校验和持久化
- [x] 建立 PWA 页面、卡片、弹窗、Toast 的全局响应式规范
- [x] 修正未来预测聚合逻辑，引入证据与不确定性
- [x] 让知识图谱路径和信源证据进入辅助决策计算与解释
- [x] 补充测试、浏览器截图与回归结果

## 实施边界

- 保留工作树中已有未提交改动，不回退场景演化和信源调度功能。
- 预测结果必须暴露假设、证据和不确定性，不使用未经校准的伪精确结论。
- 全站 PWA 优化先建立共享基础设施，再迁移高频页面，避免逐页复制样式。

## 记录

- `/auth`：夜景弯月左移，流星双向生成；日景加入太阳、云与双帧飞鸟；底部道具扩大并在树层之后绘制。
- 法务：新增 `2026-07-28` 版服务条款和隐私说明。普通注册、验证码注册、OAuth 注册均由后端校验版本，并记录同意时间。
- PWA：共享 Card、Dialog、Toast、AppShell 增加窄屏、安全区、动态视口高度和溢出处理；Service Worker 在本地开发绕过缓存，避免旧 chunk 导致 hydration 失效。
- 预测：要求改为加权几何准备度，风险改为带相关性折减的生存项；证据质量只改变 Monte Carlo 区间宽度。
- 图谱：修复 Neo4j 关系权重写错节点的问题；推演结果新增 `model_version`、`assumptions`、`evidence_summary` 和 `graph_paths`。
- 验证：后端 `7 passed`；前端 `tsc --noEmit` 通过；Next 生产构建通过并生成 20 个路由；390x844 与 1440x900 亮/暗场景无横向溢出。

## 后续校准

- 当前模型是可解释的结构化情景模型，不是经过历史样本校准的客观预测器。
- 下一阶段需要记录预测快照与真实结果，按目标类型、地区和时间窗做 Brier Score、校准曲线和漂移监控。
- 知识图谱仍需补充边的有效期、权限、冲突关系与人工确认动作，才能形成完整的操作型决策本体。
